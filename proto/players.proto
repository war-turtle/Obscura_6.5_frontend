syntax = "proto3";

package players;

option go_package = "pbPlayers";

service Player {
    rpc GetPlayers(PlayerRequest) returns (PlayerResponse) {}
}

message PlayerRequest {
    int32 id = 1;
}

message PlayerResponse {
    int32 id = 1;
}